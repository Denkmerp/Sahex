
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Picked = "None"
local Toggle1 =  false
local Toggle2 = false

local function autoRb()
    while true do 
        wait(0.25)

        if  Toggle2 == true then
            local Event = game:GetService("ReplicatedStorage").RebirthEvent
            Event:FireServer()
         end
    end
end

local function autoLvl()
    while true do 
        wait(0.25)

        if  Toggle1 == true then
            local Event = game:GetService("ReplicatedStorage").MouseClicked
            Event:FireServer()
         end
    end
end

local function A1()
    local Window = Rayfield:CreateWindow({
        Name = "SAHEX MAIN [A1]",
        Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
        LoadingTitle = "SAHEX-LOL",
        LoadingSubtitle = "by Zah",
        ShowText = "Sahex", -- for mobile users to unhide Rayfield, change if you'd like
        Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

        ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

        DisableRayfieldPrompts = false,
        DisableBuildWarnings = false, -- Prevents Rayfield from emitting warnings when the script has a version mismatch with the interface.

   -- ScriptID = "sid_xxxxxxxxxxxx", -- Your Script ID from developer.sirius.menu — enables analytics, managed keys, and script hosting

        ConfigurationSaving = {
            Enabled = true,
            FolderName = nil, -- Create a custom folder for your hub/game
            FileName = "Big Hub"
         },

        Discord = {
            Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
            Invite = "noinvitelink", -- The Discord invite code, do not include Discord.gg/. E.g. Discord.gg/ABCD would be ABCD
            RememberJoins = true -- Set this to false to make them join the Discord every time they load it up
        },

        KeySystem = false, -- Set this to true to use our key system
        KeySettings = {
        Title = "Untitled",
        Subtitle = "Key System",
        Note = "No method of obtaining the key is provided", -- Use this to tell the user how to get a key
        FileName = "Key", -- It is recommended to use something unique, as other scripts using Rayfield may overwrite your key file
        SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
        GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
        Key = {"Hello"} -- List of keys that the system will accept, can be RAW file links (pastebin, github, etc.) or simple strings ("hello", "key22")
        }
    })

    local  MainTab = Window:CreateTab("Main", nil) -- Title, Image
    local Fun = Window:CreateTab("FunStuff")
    local Credits = Window:CreateTab("Credits")

    local Label = MainTab:CreateLabel("Auto", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

    local Toggle = MainTab:CreateToggle({
        Name = "AUTO LEVEL",
        CurrentValue = false,
        Flag = "Toggle1", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
        Callback = function(state)
            if state then
                Toggle1 = true
                autoLvl()
            else
                Toggle1 = false
            end
        end,
    })

    local Toggle = MainTab:CreateToggle({
        Name = "AUTO REBIRTH",
        CurrentValue = false,
        Flag = "Toggle2", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
        Callback = function(state)
            if state then
                Toggle2 = true
                autoRb()
            else
                Toggle2 = false
            end
        end,
    })

    local Label2 = MainTab:CreateLabel("Player", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

    local Slider1 = MainTab:CreateSlider({
        Name = "Walkspeed",
        Range = {16, 100},
        Increment = 1,
        Suffix = "Speed",
        CurrentValue = 16,
        Flag = "Slider1", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
        Callback = function(Value)
            local LocalPlayer = game.Players.LocalPlayer

            while true do
                wait(0.15)
                game.Workspace:FindFirstChild(LocalPlayer.Name).Humanoid.WalkSpeed = Value
            end
        end,
    })

    local Slider2 = MainTab:CreateSlider({
        Name = "Jump Power",
        Range = {6, 40},
        Increment = 1,
        Suffix = "Jump Power",
        CurrentValue = 6,
        Flag = "Slider2", -- A flag is the identifier for the configuration file; make sure every element has a different flag if you're using configuration saving to ensure no overlaps
        Callback = function(Value)
            local LocalPlayer = game.Players.LocalPlayer

            while true do
                wait(0.15)
                game.Workspace:FindFirstChild(LocalPlayer.Name).Humanoid.JumpHeight = Value
            end
        end,
    })

    local Label3 = MainTab:CreateLabel("Obby", nil, Color3.fromRGB(255, 255, 255), false) -- Title, Icon, Color, IgnoreTheme

    local Button = MainTab:CreateButton({
        Name = "Destroy Tsunami",
        Callback = function()
            while true do
                wait(0.15)
                game.Workspace:WaitForChild("MinigameTsunami"):Destroy()
            end
        end,
    })

    local Label4 = Fun:CreateLabel("Gender", nil, Color3.fromRGB(255, 255, 255), false)

    local Boy = Fun:CreateButton({
        Name = "Turn into boy",
        Callback = function()

            local Event = game:GetService("ReplicatedStorage").BoyEvent
            Event:FireServer(
            game:GetService("Players").LocalPlayer
            )
        end,
    })

    local Girl = Fun:CreateButton({
        Name = "Turn into Girl",
        Callback = function()

            local Event = game:GetService("ReplicatedStorage").GirlEvent
            Event:FireServer(
            game:GetService("Players").LocalPlayer
            )
        end,
    })

local Label5 = Fun:CreateLabel("Money Grind", nil, Color3.fromRGB(255, 255, 255), false)


    local AutoPod = Fun:CreateDropdown({
        Name = "Choose Pod (You cant join minigames)",
        Options = {"None", "Basic", "Double", "Gold", "Diamond", "Emerald", "Molten", "Ruby", "Amethyst", "Sapphire", "Double Motlen"},
        CurrentOption = {"None"},
        MultipleOptions = false,
        Callback = function(Options)
            print(Options)

            Picked = Options
        end,
    })

    local Label6 = Fun:CreateLabel("Made by ZUHZUHLAH", nil, Color3.fromRGB(255, 255, 255), false)
    local Label7 = Fun:CreateLabel("sites.google.com/view/sahex-lol", nil, Color3.fromRGB(255, 255, 255), false)

    Rayfield:LoadConfiguration()
end

if game.PlaceId == 130526979296684 then
    A1()
else
     local StarterGui = game:GetService("StarterGui")

    local function notifyWithCopy(title, message, clipboardText)
        if clipboardText and setclipboard then
            setclipboard(clipboardText)
        end
    
        StarterGui:SetCore("SendNotification", {
            Title = title or "Notification";
            Text = message or "Check your clipboard!";
            Duration = 10;
            Button1 = "Okay";
        })
    
            print("📋 Copied to clipboard: " .. (clipboardText or message))
        end

    notifyWithCopy("Game Not supported", "This game is not supported. We copied our website to your cliboard check our games we support.", "sites.google.com/view/sahex-lol/livegames")

    print("Official Roblox notifications script loaded with clipboard copy!")

    local AlarmSound = Instance.new("Sound") 
    AlarmSound.Parent = game.Workspace
    AlarmSound.SoundId = "rbxassetid://6432593850"
    AlarmSound.Playing = true
    AlarmSound.Volume = 1
end
